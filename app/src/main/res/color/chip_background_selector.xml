<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado selecionado -->
    <item android:state_checked="true" android:color="@color/chip_background_selected" />

    <!-- Estado pressionado -->
    <item android:state_pressed="true" android:color="@color/light_blue" />

    <!-- Estado padrão -->
    <item android:color="@color/chip_background_default" />
</selector>